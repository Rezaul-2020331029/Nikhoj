spring:
  application:
    name: Config-Service

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs


server:
  port: 7002